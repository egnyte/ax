package kibana

import "testing"

func TestProject(t *testing.T) {
	/*myMap := project(map[string]interface{}{
		"name": "Hello",
		"age":  33,
		"docker": map[string]interface{}{
			"service": "bla",
		},
	}, []string{"age", "docker.service"})
	if !reflect.DeepEqual(myMap, map[string]interface{}{
		"age":            33,
		"docker.service": "bla",
	}) {
		t.Fatalf("Can't properly map: %+v", myMap)
	}
	*/
}
